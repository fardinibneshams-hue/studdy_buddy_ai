## Packages
framer-motion | Smooth transitions and playful animations
lucide-react | Beautiful icons for the UI
clsx | Conditional class merging
tailwind-merge | Utility for merging tailwind classes

## Notes
The backend provides endpoints for PDF upload, summarization, Q&A, and Quiz generation.
Authentication is a simple password check.
TTS will use the browser's `window.speechSynthesis` API (no external package needed).
